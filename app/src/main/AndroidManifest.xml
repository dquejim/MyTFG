<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mytfg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_foreground"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MyTFG">
        <activity
            android:name=".Views.Login_Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
            <activity android:name=".Views.NewAccount_Activity"> </activity>
            <activity android:name=".Views.Home_Activity"></activity>
            <activity android:name=".Views.PersonalizeUser_Activity"></activity>
            <activity android:name=".Views.Food_Activity"></activity>
            <activity android:name=".Views.popUpActivity" android:theme="@style/PopUpStyle"></activity>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>